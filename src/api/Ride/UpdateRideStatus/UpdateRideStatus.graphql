type UpdateRideStatusResponse {
  ok: Boolean!
  error: String
  rideId: Int
}

enum StatusOpeions {
  ACCEPTED
  FINISHED
  CANCELED
  REQUESTING
  ONROUTE
}

type Mutation {
  UpdateRideStatus(
    rideId: Int!
    status: StatusOpeions!
  ): UpdateRideStatusResponse!
}
