type UpdateRideStatusResponse {
  ok: Boolean!
  error: String
}

enum StatusOpeions {
  ACCEPTED
  FINISHED
  CANCELED
  REQUESTING
  ONROUTE
}

type Mutation {
  UpdateRideStatus(
    rideId: Int!
    status: StatusOpeions!
  ): UpdateRideStatusResponse!
}
